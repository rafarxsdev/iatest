# Servidor Astro (SSR/middleware) llama al API aquí:
BACKEND_URL=http://localhost:3000
# Navegador: dejar vacío para usar el mismo origen que el frontend (:4321) vía proxy de Vite.
# Solo rellenar si sirves API y web bajo el mismo host con otro esquema.
PUBLIC_BACKEND_URL=
PUBLIC_APP_TITLE=Dashboard
